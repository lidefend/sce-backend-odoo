<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="action_sc_equipment_plan" model="ir.actions.act_window">
        <field name="name">设备计划</field>
        <field name="res_model">sc.equipment.plan</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_project': 1}</field>
    </record>

    <record id="action_sc_equipment_request" model="ir.actions.act_window">
        <field name="name">设备申请</field>
        <field name="res_model">sc.equipment.request</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_project': 1}</field>
    </record>

    <record id="action_sc_equipment_usage" model="ir.actions.act_window">
        <field name="name">机械台班登记</field>
        <field name="res_model">sc.equipment.usage</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_project': 1}</field>
        <field name="groups_id" eval="[(6, 0, [ref('smart_construction_core.group_sc_cap_project_read')])]"/>
    </record>

    <record id="action_sc_equipment_settlement" model="ir.actions.act_window">
        <field name="name">设备结算</field>
        <field name="res_model">sc.equipment.settlement</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_project': 1}</field>
    </record>

    <record id="action_sc_equipment_price" model="ir.actions.act_window">
        <field name="name">设备价格库</field>
        <field name="res_model">sc.equipment.price</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_active': 1}</field>
    </record>

    <record id="view_sc_equipment_plan_tree" model="ir.ui.view">
        <field name="name">sc.equipment.plan.tree</field>
        <field name="model">sc.equipment.plan</field>
        <field name="arch" type="xml">
            <tree string="设备计划" default_order="plan_date desc">
                <field name="name"/>
                <field name="project_id"/>
                <field name="plan_date"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="usage_location"/>
                <field name="owner_id"/>
                <field name="supplier_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sc_equipment_plan_form" model="ir.ui.view">
        <field name="name">sc.equipment.plan.form</field>
        <field name="model">sc.equipment.plan</field>
        <field name="arch" type="xml">
            <form string="设备计划">
                <header>
                    <button name="action_submit" string="提交" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve" string="确认计划" type="object" class="btn-primary" invisible="state != 'submitted'"/>
                    <button name="action_reset_draft" string="退回草稿" type="object" invisible="state != 'submitted'"/>
                    <button name="action_cancel" string="取消" type="object" invisible="state in ('approved', 'cancel')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="project_id"/>
                            <field name="plan_date"/>
                            <field name="start_date"/>
                            <field name="end_date"/>
                        </group>
                        <group>
                            <field name="usage_location"/>
                            <field name="owner_id"/>
                            <field name="supplier_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="计划明细">
                            <field name="line_ids">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="equipment_name"/>
                                    <field name="equipment_code"/>
                                    <field name="planned_qty"/>
                                    <field name="planned_hours"/>
                                    <field name="usage_location"/>
                                    <field name="operator_requirement"/>
                                    <field name="note"/>
                                </tree>
                            </field>
                        </page>
                        <page string="计划说明">
                            <field name="note" nolabel="1"/>
                        </page>
                        <page string="来源追溯">
                            <group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_sc_equipment_plan_search" model="ir.ui.view">
        <field name="name">sc.equipment.plan.search</field>
        <field name="model">sc.equipment.plan</field>
        <field name="arch" type="xml">
            <search string="设备计划">
                <field name="name"/>
                <field name="project_id"/>
                <field name="plan_date"/>
                <field name="usage_location"/>
                <field name="owner_id"/>
                <field name="supplier_id"/>
                <filter string="草稿" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="已提交" name="submitted" domain="[('state', '=', 'submitted')]"/>
                <filter string="已确认" name="approved" domain="[('state', '=', 'approved')]"/>
                <filter string="按项目" name="group_project" context="{'group_by': 'project_id'}"/>
                <filter string="按负责人" name="group_owner" context="{'group_by': 'owner_id'}"/>
                <filter string="按供应单位" name="group_supplier" context="{'group_by': 'supplier_id'}"/>
                <filter string="按状态" name="group_state" context="{'group_by': 'state'}"/>
            </search>
        </field>
    </record>

    <record id="view_sc_equipment_request_tree" model="ir.ui.view">
        <field name="name">sc.equipment.request.tree</field>
        <field name="model">sc.equipment.request</field>
        <field name="arch" type="xml">
            <tree string="设备申请" default_order="request_date desc">
                <field name="name"/>
                <field name="project_id"/>
                <field name="plan_id"/>
                <field name="request_date"/>
                <field name="required_date"/>
                <field name="usage_location"/>
                <field name="requester_id"/>
                <field name="supplier_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sc_equipment_request_form" model="ir.ui.view">
        <field name="name">sc.equipment.request.form</field>
        <field name="model">sc.equipment.request</field>
        <field name="arch" type="xml">
            <form string="设备申请">
                <header>
                    <button name="action_submit" string="提交" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve" string="确认申请" type="object" class="btn-primary" invisible="state != 'submitted'"/>
                    <button name="action_reset_draft" string="退回草稿" type="object" invisible="state != 'submitted'"/>
                    <button name="action_cancel" string="取消" type="object" invisible="state in ('approved', 'cancel')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="project_id"/>
                            <field name="plan_id"/>
                            <field name="request_date"/>
                            <field name="required_date"/>
                        </group>
                        <group>
                            <field name="usage_location"/>
                            <field name="requester_id"/>
                            <field name="supplier_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="申请明细">
                            <field name="line_ids">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="equipment_name"/>
                                    <field name="equipment_code"/>
                                    <field name="requested_qty"/>
                                    <field name="planned_hours"/>
                                    <field name="usage_location"/>
                                    <field name="operator_requirement"/>
                                    <field name="note"/>
                                </tree>
                            </field>
                        </page>
                        <page string="申请说明">
                            <field name="note" nolabel="1"/>
                        </page>
                        <page string="来源追溯">
                            <group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_sc_equipment_request_search" model="ir.ui.view">
        <field name="name">sc.equipment.request.search</field>
        <field name="model">sc.equipment.request</field>
        <field name="arch" type="xml">
            <search string="设备申请">
                <field name="name"/>
                <field name="project_id"/>
                <field name="plan_id"/>
                <field name="request_date"/>
                <field name="required_date"/>
                <field name="usage_location"/>
                <field name="requester_id"/>
                <field name="supplier_id"/>
                <filter string="草稿" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="已提交" name="submitted" domain="[('state', '=', 'submitted')]"/>
                <filter string="已确认" name="approved" domain="[('state', '=', 'approved')]"/>
                <filter string="按项目" name="group_project" context="{'group_by': 'project_id'}"/>
                <filter string="按来源计划" name="group_plan" context="{'group_by': 'plan_id'}"/>
                <filter string="按申请人" name="group_requester" context="{'group_by': 'requester_id'}"/>
                <filter string="按供应单位" name="group_supplier" context="{'group_by': 'supplier_id'}"/>
                <filter string="按状态" name="group_state" context="{'group_by': 'state'}"/>
            </search>
        </field>
    </record>

    <record id="view_sc_equipment_usage_tree" model="ir.ui.view">
        <field name="name">sc.equipment.usage.tree</field>
        <field name="model">sc.equipment.usage</field>
        <field name="arch" type="xml">
            <tree string="机械台班登记" default_order="usage_date desc">
                <field name="name"/>
                <field name="project_id"/>
                <field name="request_id"/>
                <field name="usage_date"/>
                <field name="equipment_name"/>
                <field name="equipment_code"/>
                <field name="specification"/>
                <field name="uom_text"/>
                <field name="usage_location"/>
                <field name="operator_name"/>
                <field name="usage_qty" sum="使用台数合计"/>
                <field name="usage_hours"/>
                <field name="price_unit"/>
                <field name="amount" sum="使用金额合计"/>
                <field name="supplier_id"/>
                <field name="recorder_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sc_equipment_usage_form" model="ir.ui.view">
        <field name="name">sc.equipment.usage.form</field>
        <field name="model">sc.equipment.usage</field>
        <field name="arch" type="xml">
            <form string="机械台班登记">
                <header>
                    <button name="action_submit" string="提交" type="object" class="btn-primary" invisible="state != 'draft'" groups="smart_construction_core.group_sc_cap_project_user,smart_construction_core.group_sc_cap_project_manager"/>
                    <button name="action_confirm" string="确认台班" type="object" class="btn-primary" invisible="state != 'submitted'" groups="smart_construction_core.group_sc_cap_project_manager"/>
                    <button name="action_reset_draft" string="退回草稿" type="object" invisible="state != 'cancel'" groups="smart_construction_core.group_sc_cap_project_manager"/>
                    <button name="action_cancel" string="取消" type="object" invisible="state in ('confirmed', 'cancel')" groups="smart_construction_core.group_sc_cap_project_user,smart_construction_core.group_sc_cap_project_manager"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,confirmed"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="project_id" readonly="state != 'draft'"/>
                            <field name="request_id" readonly="state != 'draft'"/>
                            <field name="usage_date" readonly="state != 'draft'"/>
                            <field name="equipment_name" readonly="state != 'draft'"/>
                            <field name="equipment_code" readonly="state != 'draft'"/>
                            <field name="specification" readonly="state != 'draft'"/>
                            <field name="uom_text" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="usage_location" readonly="state != 'draft'"/>
                            <field name="operator_name" readonly="state != 'draft'"/>
                            <field name="usage_qty" readonly="state != 'draft'"/>
                            <field name="usage_hours" readonly="state != 'draft'"/>
                            <field name="currency_id" readonly="state != 'draft'" groups="base.group_multi_currency"/>
                            <field name="price_unit" readonly="state != 'draft'"/>
                            <field name="amount" readonly="1"/>
                            <field name="supplier_id" readonly="state != 'draft'"/>
                            <field name="recorder_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="使用说明">
                            <field name="note" nolabel="1"/>
                        </page>
                        <page string="附件">
                            <field name="attachment_ids" widget="many2many_binary" nolabel="1"/>
                        </page>
                        <page string="办理提示">
                            <field name="processing_advisory" readonly="1" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_sc_equipment_usage_search" model="ir.ui.view">
        <field name="name">sc.equipment.usage.search</field>
        <field name="model">sc.equipment.usage</field>
        <field name="arch" type="xml">
            <search string="机械台班登记">
                <field name="name"/>
                <field name="project_id"/>
                <field name="request_id"/>
                <field name="usage_date"/>
                <field name="equipment_name"/>
                <field name="equipment_code"/>
                <field name="usage_location"/>
                <field name="operator_name"/>
                <field name="supplier_id"/>
                <field name="recorder_id"/>
                <filter string="草稿" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="已提交" name="submitted" domain="[('state', '=', 'submitted')]"/>
                <filter string="已确认" name="confirmed" domain="[('state', '=', 'confirmed')]"/>
                <filter string="按项目" name="group_project" context="{'group_by': 'project_id'}"/>
                <filter string="按日期" name="group_usage_date" context="{'group_by': 'usage_date'}"/>
                <filter string="按来源申请" name="group_request" context="{'group_by': 'request_id'}"/>
                <filter string="按设备名称" name="group_equipment" context="{'group_by': 'equipment_name'}"/>
                <filter string="按供应单位" name="group_supplier" context="{'group_by': 'supplier_id'}"/>
                <filter string="按状态" name="group_state" context="{'group_by': 'state'}"/>
            </search>
        </field>
    </record>

    <record id="view_sc_equipment_settlement_tree" model="ir.ui.view">
        <field name="name">sc.equipment.settlement.tree</field>
        <field name="model">sc.equipment.settlement</field>
        <field name="arch" type="xml">
            <tree string="设备结算" default_order="settlement_date desc">
                <field name="name"/>
                <field name="project_id"/>
                <field name="settlement_date"/>
                <field name="supplier_id"/>
                <field name="amount_untaxed" sum="未税金额合计"/>
                <field name="tax_amount" sum="税额合计"/>
                <field name="amount_total" sum="结算金额合计"/>
                <field name="currency_id" invisible="1"/>
                <field name="owner_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sc_equipment_settlement_form" model="ir.ui.view">
        <field name="name">sc.equipment.settlement.form</field>
        <field name="model">sc.equipment.settlement</field>
        <field name="arch" type="xml">
            <form string="设备结算">
                <header>
                    <button name="action_submit" string="提交" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_confirm" string="确认结算" type="object" class="btn-primary" invisible="state != 'submitted'"/>
                    <button name="action_reset_draft" string="退回草稿" type="object" invisible="state != 'submitted'"/>
                    <button name="action_cancel" string="取消" type="object" invisible="state in ('confirmed', 'cancel')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,confirmed"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="project_id"/>
                            <field name="supplier_id"/>
                            <field name="settlement_date"/>
                        </group>
                        <group>
                            <field name="owner_id"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="amount_untaxed"/>
                            <field name="tax_amount"/>
                            <field name="amount_total"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="结算明细">
                            <field name="line_ids">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="usage_id"/>
                                    <field name="equipment_name"/>
                                    <field name="equipment_code"/>
                                    <field name="qty"/>
                                    <field name="unit_name"/>
                                    <field name="unit_price"/>
                                    <field name="tax_rate"/>
                                    <field name="amount_untaxed"/>
                                    <field name="tax_amount"/>
                                    <field name="amount_total"/>
                                    <field name="note"/>
                                </tree>
                            </field>
                        </page>
                        <page string="结算说明">
                            <field name="note" nolabel="1"/>
                        </page>
                        <page string="来源追溯">
                            <group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_sc_equipment_settlement_search" model="ir.ui.view">
        <field name="name">sc.equipment.settlement.search</field>
        <field name="model">sc.equipment.settlement</field>
        <field name="arch" type="xml">
            <search string="设备结算">
                <field name="name"/>
                <field name="project_id"/>
                <field name="supplier_id"/>
                <field name="settlement_date"/>
                <field name="owner_id"/>
                <filter string="草稿" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="已提交" name="submitted" domain="[('state', '=', 'submitted')]"/>
                <filter string="已确认" name="confirmed" domain="[('state', '=', 'confirmed')]"/>
                <filter string="按项目" name="group_project" context="{'group_by': 'project_id'}"/>
                <filter string="按供应单位" name="group_supplier" context="{'group_by': 'supplier_id'}"/>
                <filter string="按状态" name="group_state" context="{'group_by': 'state'}"/>
            </search>
        </field>
    </record>

    <record id="view_sc_equipment_price_tree" model="ir.ui.view">
        <field name="name">sc.equipment.price.tree</field>
        <field name="model">sc.equipment.price</field>
        <field name="arch" type="xml">
            <tree string="设备价格库" default_order="effective_date desc">
                <field name="name"/>
                <field name="project_id"/>
                <field name="supplier_id"/>
                <field name="equipment_name"/>
                <field name="equipment_code"/>
                <field name="unit_name"/>
                <field name="unit_price"/>
                <field name="tax_rate"/>
                <field name="effective_date"/>
                <field name="expire_date"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sc_equipment_price_form" model="ir.ui.view">
        <field name="name">sc.equipment.price.form</field>
        <field name="model">sc.equipment.price</field>
        <field name="arch" type="xml">
            <form string="设备价格库">
                <header>
                    <button name="action_activate" string="生效" type="object" class="btn-primary" invisible="state == 'active'"/>
                    <button name="action_deactivate" string="停用" type="object" invisible="state != 'active'"/>
                    <button name="action_reset_draft" string="退回草稿" type="object" invisible="state == 'draft'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,inactive"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="project_id"/>
                            <field name="supplier_id"/>
                            <field name="equipment_name"/>
                            <field name="equipment_code"/>
                        </group>
                        <group>
                            <field name="unit_name"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="unit_price"/>
                            <field name="tax_rate"/>
                            <field name="effective_date"/>
                            <field name="expire_date"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="价格说明">
                            <field name="note" nolabel="1"/>
                        </page>
                        <page string="来源追溯">
                            <group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_sc_equipment_price_search" model="ir.ui.view">
        <field name="name">sc.equipment.price.search</field>
        <field name="model">sc.equipment.price</field>
        <field name="arch" type="xml">
            <search string="设备价格库">
                <field name="name"/>
                <field name="project_id"/>
                <field name="supplier_id"/>
                <field name="equipment_name"/>
                <field name="equipment_code"/>
                <filter string="生效" name="active" domain="[('state', '=', 'active')]"/>
                <filter string="草稿" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="停用" name="inactive" domain="[('state', '=', 'inactive')]"/>
                <filter string="按项目" name="group_project" context="{'group_by': 'project_id'}"/>
                <filter string="按供应单位" name="group_supplier" context="{'group_by': 'supplier_id'}"/>
                <filter string="按设备名称" name="group_equipment" context="{'group_by': 'equipment_name'}"/>
                <filter string="按状态" name="group_state" context="{'group_by': 'state'}"/>
            </search>
        </field>
    </record>
</odoo>
