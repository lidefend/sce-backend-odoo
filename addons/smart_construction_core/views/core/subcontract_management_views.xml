<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="action_sc_subcontract_plan" model="ir.actions.act_window">
        <field name="name">分包计划</field>
        <field name="res_model">sc.subcontract.plan</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_project': 1}</field>
    </record>

    <record id="view_sc_subcontract_plan_tree" model="ir.ui.view">
        <field name="name">sc.subcontract.plan.tree</field>
        <field name="model">sc.subcontract.plan</field>
        <field name="arch" type="xml">
            <tree string="分包计划" default_order="plan_date desc">
                <field name="name"/>
                <field name="project_id"/>
                <field name="contract_id"/>
                <field name="plan_date"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="subcontract_scope"/>
                <field name="subcontractor_id"/>
                <field name="estimated_amount" sum="预计金额合计"/>
                <field name="currency_id" invisible="1"/>
                <field name="owner_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sc_subcontract_plan_form" model="ir.ui.view">
        <field name="name">sc.subcontract.plan.form</field>
        <field name="model">sc.subcontract.plan</field>
        <field name="arch" type="xml">
            <form string="分包计划">
                <header>
                    <button name="action_submit" string="提交" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve" string="确认计划" type="object" class="btn-primary" invisible="state != 'submitted'"/>
                    <button name="action_reset_draft" string="退回草稿" type="object" invisible="state != 'submitted'"/>
                    <button name="action_cancel" string="取消" type="object" invisible="state in ('approved', 'cancel')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="project_id"/>
                            <field name="contract_id"/>
                            <field name="plan_date"/>
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="subcontract_scope"/>
                        </group>
                        <group>
                            <field name="subcontractor_id"/>
                            <field name="owner_id"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="estimated_amount"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="计划明细">
                            <field name="line_ids">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="work_scope"/>
                                    <field name="work_content"/>
                                    <field name="planned_qty"/>
                                    <field name="unit_name"/>
                                    <field name="estimated_amount"/>
                                    <field name="note"/>
                                </tree>
                            </field>
                        </page>
                        <page string="计划说明">
                            <field name="note" nolabel="1"/>
                        </page>
                        <page string="来源追溯">
                            <group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_sc_subcontract_plan_search" model="ir.ui.view">
        <field name="name">sc.subcontract.plan.search</field>
        <field name="model">sc.subcontract.plan</field>
        <field name="arch" type="xml">
            <search string="分包计划">
                <field name="name"/>
                <field name="project_id"/>
                <field name="contract_id"/>
                <field name="plan_date"/>
                <field name="subcontract_scope"/>
                <field name="subcontractor_id"/>
                <field name="owner_id"/>
                <filter string="草稿" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="已提交" name="submitted" domain="[('state', '=', 'submitted')]"/>
                <filter string="已确认" name="approved" domain="[('state', '=', 'approved')]"/>
                <filter string="按项目" name="group_project" context="{'group_by': 'project_id'}"/>
                <filter string="按关联合同" name="group_contract" context="{'group_by': 'contract_id'}"/>
                <filter string="按分包单位" name="group_subcontractor" context="{'group_by': 'subcontractor_id'}"/>
                <filter string="按状态" name="group_state" context="{'group_by': 'state'}"/>
            </search>
        </field>
    </record>

    <record id="action_sc_subcontract_request" model="ir.actions.act_window">
        <field name="name">分包申请</field>
        <field name="res_model">sc.subcontract.request</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_project': 1}</field>
    </record>

    <record id="view_sc_subcontract_request_tree" model="ir.ui.view">
        <field name="name">sc.subcontract.request.tree</field>
        <field name="model">sc.subcontract.request</field>
        <field name="arch" type="xml">
            <tree string="分包申请" default_order="request_date desc">
                <field name="name"/>
                <field name="project_id"/>
                <field name="plan_id"/>
                <field name="contract_id"/>
                <field name="request_date"/>
                <field name="need_start_date"/>
                <field name="need_end_date"/>
                <field name="subcontract_scope"/>
                <field name="suggested_subcontractor_id"/>
                <field name="estimated_amount" sum="预计金额合计"/>
                <field name="priority"/>
                <field name="applicant_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sc_subcontract_request_form" model="ir.ui.view">
        <field name="name">sc.subcontract.request.form</field>
        <field name="model">sc.subcontract.request</field>
        <field name="arch" type="xml">
            <form string="分包申请">
                <header>
                    <button name="action_submit" string="提交" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve" string="确认申请" type="object" class="btn-primary" invisible="state != 'submitted'"/>
                    <button name="action_reset_draft" string="退回草稿" type="object" invisible="state != 'submitted'"/>
                    <button name="action_cancel" string="取消" type="object" invisible="state in ('approved', 'cancel')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="project_id"/>
                            <field name="plan_id"/>
                            <field name="contract_id"/>
                            <field name="request_date"/>
                            <field name="need_start_date"/>
                            <field name="need_end_date"/>
                            <field name="subcontract_scope"/>
                        </group>
                        <group>
                            <field name="suggested_subcontractor_id"/>
                            <field name="applicant_id"/>
                            <field name="department_id"/>
                            <field name="priority"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="estimated_amount"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="申请明细">
                            <field name="line_ids">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="work_scope"/>
                                    <field name="work_content"/>
                                    <field name="required_qty"/>
                                    <field name="unit_name"/>
                                    <field name="required_date"/>
                                    <field name="estimated_amount"/>
                                    <field name="note"/>
                                </tree>
                            </field>
                        </page>
                        <page string="申请原因">
                            <field name="request_reason" nolabel="1"/>
                        </page>
                        <page string="附件">
                            <field name="attachment_ids" widget="many2many_binary" nolabel="1"/>
                        </page>
                        <page string="备注">
                            <field name="note" nolabel="1"/>
                        </page>
                        <page string="来源追溯">
                            <group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_sc_subcontract_request_search" model="ir.ui.view">
        <field name="name">sc.subcontract.request.search</field>
        <field name="model">sc.subcontract.request</field>
        <field name="arch" type="xml">
            <search string="分包申请">
                <field name="name"/>
                <field name="project_id"/>
                <field name="plan_id"/>
                <field name="contract_id"/>
                <field name="request_date"/>
                <field name="subcontract_scope"/>
                <field name="suggested_subcontractor_id"/>
                <field name="applicant_id"/>
                <filter string="草稿" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="已提交" name="submitted" domain="[('state', '=', 'submitted')]"/>
                <filter string="已确认" name="approved" domain="[('state', '=', 'approved')]"/>
                <filter string="紧急" name="urgent" domain="[('priority', '=', 'urgent')]"/>
                <filter string="按项目" name="group_project" context="{'group_by': 'project_id'}"/>
                <filter string="按日期" name="group_request_date" context="{'group_by': 'request_date'}"/>
                <filter string="按分包计划" name="group_plan" context="{'group_by': 'plan_id'}"/>
                <filter string="按关联合同" name="group_contract" context="{'group_by': 'contract_id'}"/>
                <filter string="按建议分包单位" name="group_subcontractor" context="{'group_by': 'suggested_subcontractor_id'}"/>
                <filter string="按状态" name="group_state" context="{'group_by': 'state'}"/>
            </search>
        </field>
    </record>

    <record id="action_sc_subcontract_register" model="ir.actions.act_window">
        <field name="name">分包登记</field>
        <field name="res_model">sc.subcontract.register</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_project': 1}</field>
    </record>

    <record id="view_sc_subcontract_register_tree" model="ir.ui.view">
        <field name="name">sc.subcontract.register.tree</field>
        <field name="model">sc.subcontract.register</field>
        <field name="arch" type="xml">
            <tree string="分包登记" default_order="register_date desc">
                <field name="name"/>
                <field name="project_id"/>
                <field name="request_id"/>
                <field name="contract_id"/>
                <field name="register_date"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="subcontract_scope"/>
                <field name="subcontractor_id"/>
                <field name="registered_amount" sum="登记金额合计"/>
                <field name="responsible_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sc_subcontract_register_form" model="ir.ui.view">
        <field name="name">sc.subcontract.register.form</field>
        <field name="model">sc.subcontract.register</field>
        <field name="arch" type="xml">
            <form string="分包登记">
                <header>
                    <button name="action_register" string="确认登记" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_close" string="关闭" type="object" class="btn-primary" invisible="state != 'active'"/>
                    <button name="action_reset_draft" string="退回草稿" type="object" invisible="state != 'active'"/>
                    <button name="action_cancel" string="取消" type="object" invisible="state in ('closed', 'cancel')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,closed"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="project_id"/>
                            <field name="request_id"/>
                            <field name="contract_id"/>
                            <field name="register_date"/>
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="subcontract_scope"/>
                        </group>
                        <group>
                            <field name="subcontractor_id"/>
                            <field name="responsible_id"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="registered_amount"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="登记明细">
                            <field name="line_ids">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="work_scope"/>
                                    <field name="work_content"/>
                                    <field name="contract_qty"/>
                                    <field name="unit_name"/>
                                    <field name="registered_amount"/>
                                    <field name="note"/>
                                </tree>
                            </field>
                        </page>
                        <page string="管理要求">
                            <field name="management_note" nolabel="1"/>
                        </page>
                        <page string="备注">
                            <field name="note" nolabel="1"/>
                        </page>
                        <page string="来源追溯">
                            <group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_sc_subcontract_register_search" model="ir.ui.view">
        <field name="name">sc.subcontract.register.search</field>
        <field name="model">sc.subcontract.register</field>
        <field name="arch" type="xml">
            <search string="分包登记">
                <field name="name"/>
                <field name="project_id"/>
                <field name="request_id"/>
                <field name="contract_id"/>
                <field name="register_date"/>
                <field name="subcontract_scope"/>
                <field name="subcontractor_id"/>
                <field name="responsible_id"/>
                <filter string="草稿" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="已登记" name="active" domain="[('state', '=', 'active')]"/>
                <filter string="已关闭" name="closed" domain="[('state', '=', 'closed')]"/>
                <filter string="按项目" name="group_project" context="{'group_by': 'project_id'}"/>
                <filter string="按分包申请" name="group_request" context="{'group_by': 'request_id'}"/>
                <filter string="按分包合同" name="group_contract" context="{'group_by': 'contract_id'}"/>
                <filter string="按分包单位" name="group_subcontractor" context="{'group_by': 'subcontractor_id'}"/>
                <filter string="按状态" name="group_state" context="{'group_by': 'state'}"/>
            </search>
        </field>
    </record>

    <record id="action_sc_subcontract_settlement" model="ir.actions.act_window">
        <field name="name">分包结算</field>
        <field name="res_model">sc.subcontract.settlement</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_project': 1}</field>
    </record>

    <record id="view_sc_subcontract_settlement_tree" model="ir.ui.view">
        <field name="name">sc.subcontract.settlement.tree</field>
        <field name="model">sc.subcontract.settlement</field>
        <field name="arch" type="xml">
            <tree string="分包结算" default_order="settlement_date desc">
                <field name="name"/>
                <field name="project_id"/>
                <field name="register_id"/>
                <field name="contract_id"/>
                <field name="subcontractor_id"/>
                <field name="settlement_date"/>
                <field name="amount_untaxed" sum="未税金额合计"/>
                <field name="tax_amount" sum="税额合计"/>
                <field name="amount_total" sum="结算金额合计"/>
                <field name="owner_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sc_subcontract_settlement_form" model="ir.ui.view">
        <field name="name">sc.subcontract.settlement.form</field>
        <field name="model">sc.subcontract.settlement</field>
        <field name="arch" type="xml">
            <form string="分包结算">
                <header>
                    <button name="action_submit" string="提交" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_confirm" string="确认结算" type="object" class="btn-primary" invisible="state != 'submitted'"/>
                    <button name="action_reset_draft" string="退回草稿" type="object" invisible="state != 'submitted'"/>
                    <button name="action_cancel" string="取消" type="object" invisible="state in ('confirmed', 'cancel')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,confirmed"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="project_id"/>
                            <field name="register_id" readonly="1"/>
                            <field name="contract_id"/>
                            <field name="subcontractor_id"/>
                            <field name="settlement_date"/>
                        </group>
                        <group>
                            <field name="owner_id"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="amount_untaxed"/>
                            <field name="tax_amount"/>
                            <field name="amount_total"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="结算明细">
                            <field name="line_ids">
                                <tree editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="register_line_id"/>
                                    <field name="work_scope"/>
                                    <field name="work_content"/>
                                    <field name="qty"/>
                                    <field name="unit_name"/>
                                    <field name="unit_price"/>
                                    <field name="tax_rate"/>
                                    <field name="amount_total"/>
                                    <field name="note"/>
                                </tree>
                            </field>
                        </page>
                        <page string="结算说明">
                            <field name="note" nolabel="1"/>
                        </page>
                        <page string="来源追溯">
                            <group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_sc_subcontract_settlement_search" model="ir.ui.view">
        <field name="name">sc.subcontract.settlement.search</field>
        <field name="model">sc.subcontract.settlement</field>
        <field name="arch" type="xml">
            <search string="分包结算">
                <field name="name"/>
                <field name="project_id"/>
                <field name="register_id"/>
                <field name="contract_id"/>
                <field name="subcontractor_id"/>
                <field name="settlement_date"/>
                <field name="owner_id"/>
                <filter string="草稿" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="已提交" name="submitted" domain="[('state', '=', 'submitted')]"/>
                <filter string="已确认" name="confirmed" domain="[('state', '=', 'confirmed')]"/>
                <filter string="按项目" name="group_project" context="{'group_by': 'project_id'}"/>
                <filter string="按分包登记" name="group_register" context="{'group_by': 'register_id'}"/>
                <filter string="按分包合同" name="group_contract" context="{'group_by': 'contract_id'}"/>
                <filter string="按分包单位" name="group_subcontractor" context="{'group_by': 'subcontractor_id'}"/>
                <filter string="按状态" name="group_state" context="{'group_by': 'state'}"/>
            </search>
        </field>
    </record>

    <record id="action_sc_subcontract_price" model="ir.actions.act_window">
        <field name="name">分包价格库</field>
        <field name="res_model">sc.subcontract.price</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_project': 1}</field>
    </record>

    <record id="view_sc_subcontract_price_tree" model="ir.ui.view">
        <field name="name">sc.subcontract.price.tree</field>
        <field name="model">sc.subcontract.price</field>
        <field name="arch" type="xml">
            <tree string="分包价格库" default_order="effective_date desc">
                <field name="name"/>
                <field name="project_id"/>
                <field name="subcontractor_id"/>
                <field name="work_scope"/>
                <field name="unit_name"/>
                <field name="unit_price"/>
                <field name="tax_rate"/>
                <field name="effective_date"/>
                <field name="expire_date"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sc_subcontract_price_form" model="ir.ui.view">
        <field name="name">sc.subcontract.price.form</field>
        <field name="model">sc.subcontract.price</field>
        <field name="arch" type="xml">
            <form string="分包价格库">
                <header>
                    <button name="action_activate" string="生效" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_deactivate" string="停用" type="object" invisible="state != 'active'"/>
                    <button name="action_reset_draft" string="退回草稿" type="object" invisible="state != 'inactive'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,inactive"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="project_id"/>
                            <field name="subcontractor_id"/>
                            <field name="work_scope"/>
                            <field name="work_content"/>
                            <field name="unit_name"/>
                        </group>
                        <group>
                            <field name="currency_id" invisible="1"/>
                            <field name="unit_price"/>
                            <field name="tax_rate"/>
                            <field name="effective_date"/>
                            <field name="expire_date"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="价格说明">
                            <field name="note" nolabel="1"/>
                        </page>
                        <page string="来源追溯">
                            <group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_sc_subcontract_price_search" model="ir.ui.view">
        <field name="name">sc.subcontract.price.search</field>
        <field name="model">sc.subcontract.price</field>
        <field name="arch" type="xml">
            <search string="分包价格库">
                <field name="name"/>
                <field name="project_id"/>
                <field name="subcontractor_id"/>
                <field name="work_scope"/>
                <field name="effective_date"/>
                <filter string="草稿" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="生效" name="active" domain="[('state', '=', 'active')]"/>
                <filter string="停用" name="inactive" domain="[('state', '=', 'inactive')]"/>
                <filter string="按项目" name="group_project" context="{'group_by': 'project_id'}"/>
                <filter string="按分包单位" name="group_subcontractor" context="{'group_by': 'subcontractor_id'}"/>
                <filter string="按状态" name="group_state" context="{'group_by': 'state'}"/>
            </search>
        </field>
    </record>
</odoo>
