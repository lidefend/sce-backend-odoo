<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">
        <record id="server_action_material_outbound_on_approved" model="ir.actions.server">
            <field name="name">材料损耗审批通过回调</field>
            <field name="model_id" ref="model_sc_material_outbound"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.material.outbound" and env.context.get("active_id"):
    target_records = env["sc.material.outbound"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
                    <field name="groups_id" eval="[(6, 0, [ref('smart_construction_core.group_sc_cap_material_manager')])]" />
        </record>

        <record id="server_action_material_outbound_on_rejected" model="ir.actions.server">
            <field name="name">材料损耗审批驳回回调</field>
            <field name="model_id" ref="model_sc_material_outbound"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.material.outbound" and env.context.get("active_id"):
    target_records = env["sc.material.outbound"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
                    <field name="groups_id" eval="[(6, 0, [ref('smart_construction_core.group_sc_cap_material_manager')])]" />
        </record>
    </data>
</odoo>
