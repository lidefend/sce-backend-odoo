<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">

        <record id="server_action_receipt_income_on_approved" model="ir.actions.server">
            <field name="name">收款收入审批通过回调</field>
            <field name="model_id" ref="model_sc_receipt_income"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.receipt.income" and env.context.get("active_id"):
    target_records = env["sc.receipt.income"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
        </record>

        <record id="server_action_receipt_income_on_rejected" model="ir.actions.server">
            <field name="name">收款收入审批驳回回调</field>
            <field name="model_id" ref="model_sc_receipt_income"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.receipt.income" and env.context.get("active_id"):
    target_records = env["sc.receipt.income"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
        </record>

        <record id="server_action_payment_execution_on_approved" model="ir.actions.server">
            <field name="name">付款执行审批通过回调</field>
            <field name="model_id" ref="model_sc_payment_execution"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.payment.execution" and env.context.get("active_id"):
    target_records = env["sc.payment.execution"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
        </record>

        <record id="server_action_payment_execution_on_rejected" model="ir.actions.server">
            <field name="name">付款执行审批驳回回调</field>
            <field name="model_id" ref="model_sc_payment_execution"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.payment.execution" and env.context.get("active_id"):
    target_records = env["sc.payment.execution"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
        </record>

        <record id="server_action_invoice_registration_on_approved" model="ir.actions.server">
            <field name="name">发票登记审批通过回调</field>
            <field name="model_id" ref="model_sc_invoice_registration"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.invoice.registration" and env.context.get("active_id"):
    target_records = env["sc.invoice.registration"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
        </record>

        <record id="server_action_invoice_registration_on_rejected" model="ir.actions.server">
            <field name="name">发票登记审批驳回回调</field>
            <field name="model_id" ref="model_sc_invoice_registration"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.invoice.registration" and env.context.get("active_id"):
    target_records = env["sc.invoice.registration"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
        </record>

        <record id="server_action_financing_loan_on_approved" model="ir.actions.server">
            <field name="name">融资借款审批通过回调</field>
            <field name="model_id" ref="model_sc_financing_loan"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.financing.loan" and env.context.get("active_id"):
    target_records = env["sc.financing.loan"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
        </record>

        <record id="server_action_financing_loan_on_rejected" model="ir.actions.server">
            <field name="name">融资借款审批驳回回调</field>
            <field name="model_id" ref="model_sc_financing_loan"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.financing.loan" and env.context.get("active_id"):
    target_records = env["sc.financing.loan"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
        </record>

        <record id="server_action_self_funding_registration_on_approved" model="ir.actions.server">
            <field name="name">自筹办理审批通过回调</field>
            <field name="model_id" ref="model_sc_self_funding_registration"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.self.funding.registration" and env.context.get("active_id"):
    target_records = env["sc.self.funding.registration"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
                    <field name="groups_id" eval="[(6, 0, [ref('smart_construction_core.group_sc_cap_finance_manager')])]" />
        </record>

        <record id="server_action_self_funding_registration_on_rejected" model="ir.actions.server">
            <field name="name">自筹办理审批驳回回调</field>
            <field name="model_id" ref="model_sc_self_funding_registration"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.self.funding.registration" and env.context.get("active_id"):
    target_records = env["sc.self.funding.registration"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
                    <field name="groups_id" eval="[(6, 0, [ref('smart_construction_core.group_sc_cap_finance_manager')])]" />
        </record>

        <record id="server_action_treasury_reconciliation_on_approved" model="ir.actions.server">
            <field name="name">资金对账审批通过回调</field>
            <field name="model_id" ref="model_sc_treasury_reconciliation"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.treasury.reconciliation" and env.context.get("active_id"):
    target_records = env["sc.treasury.reconciliation"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
        </record>

        <record id="server_action_treasury_reconciliation_on_rejected" model="ir.actions.server">
            <field name="name">资金对账审批驳回回调</field>
            <field name="model_id" ref="model_sc_treasury_reconciliation"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.treasury.reconciliation" and env.context.get("active_id"):
    target_records = env["sc.treasury.reconciliation"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
        </record>

        <record id="server_action_settlement_adjustment_on_approved" model="ir.actions.server">
            <field name="name">结算调整审批通过回调</field>
            <field name="model_id" ref="model_sc_settlement_adjustment"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.settlement.adjustment" and env.context.get("active_id"):
    target_records = env["sc.settlement.adjustment"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_approved()
]]></field>
        </record>

        <record id="server_action_settlement_adjustment_on_rejected" model="ir.actions.server">
            <field name="name">结算调整审批驳回回调</field>
            <field name="model_id" ref="model_sc_settlement_adjustment"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
target_records = records or record
if not target_records and env.context.get("active_model") == "sc.settlement.adjustment" and env.context.get("active_id"):
    target_records = env["sc.settlement.adjustment"].browse(env.context["active_id"])
if target_records:
    target_records.action_on_tier_rejected()
]]></field>
        </record>

    </data>
</odoo>
