# -----------------------------------------
# Python
# -----------------------------------------
__pycache__/
*.pyc
*.pyo
*.pyd

# -----------------------------------------
# Odoo runtime
# -----------------------------------------
*.log
*.pid
.vscode/
.idea/

# -----------------------------------------
# Docker runtime data
# -----------------------------------------
/runtime/
**/postgres/
**/odoo/
**/logs/

# -----------------------------------------
# Environment & secrets

# Continue AI 配置
.env.continue
.env.continue.*
.continue/
continue_config.json

# Devtools / Continue 生成物
docs/devtools/continue/*.json
docs/devtools/continue/*.log
docs/devtools/continue/*.tmp
docs/devtools/continue/*.cache
docs/devtools/continue/*.bak
docs/devtools/continue/*.backup
docs/devtools/continue/.cache/
docs/devtools/continue/scan_results/
docs/devtools/continue/temp/

# Continue 审计产物：默认全部不入库
artifacts/continue/**

# 但允许保留目录占位
!artifacts/continue/.gitkeep
# -----------------------------------------
# Runtime environment files are always local. Only explicit templates may be
# committed to the public product repository.
.env*
!.env.example
!.env.*.example

# -----------------------------------------
# Editor junk
# -----------------------------------------
.DS_Store
Thumbs.db

# -----------------------------------------
# Compiled assets
# -----------------------------------------
*.zip
*.tar.gz
*.dump
addons/smart_construction_core/var
addons/smart_construction_core/var/validate/*.json
addons/smart_construction_core/var/validate/*.log
addons/smart_construction_core/models/var/
addons/smart_construction_core/models/var/**


# -----------------------------------------
# Node modules (未来前端整合时)
# -----------------------------------------
node_modules/
frontend/apps/web/.vite/
frontend/apps/web/dist/
frontend/apps/web/dist-dev/
frontend/apps/web/dist-clean/
frontend/apps/web/dist-release/
frontend/apps/web/dist-release-rehearsal/
frontend/apps/web/dist-production-candidate/
.runtime
config/odoo.conf

config/odoo.conf

addons/smart_construction_core/models/settlement_order.py

addons/smart_construction_core/models/settlement_order.py
boq-issues.json
issue-list.txt

cat


# accidental terminal output
/make
!/make/
!/make/*.mk
artifacts
docs/tmp/
# audit artifacts (generated)
docs/audit/*.csv
docs/audit/*.sc_*.csv
docs/audit/*.tsv
docs/audit/*.json
tmp/
.tmp/

# Customer migration assets must never enter the clean product repository.
migration_assets/
.codex_home/
.cache/
.state/
.config/
# stage execution reports
out/
# generated audit outputs
docs/audit/*.csv

# local image artifacts
*.tar.gz
*.tar.gz.sha256

# -----------------------------------------
# Dev stack runtime residue at repo root
# -----------------------------------------
# Historical deploys copied full frontend bundles (index.html,
# runtime-config.js, assets/) into the repository root. The dev stack nginx
# mounts ${FRONTEND_DIST_DIR} (frontend/apps/web/dist-dev) as its HTML root,
# so these root-level copies are never referenced by any compose file and are
# pure noise on git status. Ignore them to prevent regression.
/index.html
/runtime-config.js
/assets/
frontend/apps/web/dist-dev.bak-*/
