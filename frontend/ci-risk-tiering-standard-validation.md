# CI risk-tiering STANDARD validation probe

This non-runtime frontend document exists only on the validation branch. It
proves that a path under `frontend/**` selects the STANDARD frontend checks.
It must be removed before the audit evidence is merged.
