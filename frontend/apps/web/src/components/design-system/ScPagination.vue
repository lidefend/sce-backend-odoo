<template><TDesignPagination v-bind="{ ...$attrs, ...semanticPrimitiveIdentity('ScPagination') }" :current="current" :page-size="pageSize" :total="total" :total-content="totalContent" :disabled="disabled" :page-size-options="pageSizeOptions" @current-change="emit('update:current', $event)" @page-size-change="emit('update:pageSize', $event)" /></template>
<script setup lang="ts">import { TDesignPagination } from './tdesignPrimitiveBridge'; import { semanticPrimitiveIdentity } from './primitiveAdapter'; defineOptions({ inheritAttrs:false }); withDefaults(defineProps<{current:number; pageSize:number; total:number; totalContent?:boolean; disabled?:boolean; pageSizeOptions?:number[]}>(),{totalContent:true,pageSizeOptions:()=>[]}); const emit=defineEmits<{ 'update:current':[value:number]; 'update:pageSize':[value:number] }>();</script>
