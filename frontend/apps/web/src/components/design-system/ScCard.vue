<template><TDesignCard v-bind="{ ...$attrs, ...semanticPrimitiveIdentity('ScCard') }" :title="title" :subtitle="subtitle" :bordered="bordered"><template v-if="$slots.actions" #actions><slot name="actions" /></template><slot /></TDesignCard></template>
<script setup lang="ts">import { TDesignCard } from './tdesignPrimitiveBridge'; import { semanticPrimitiveIdentity } from './primitiveAdapter'; defineOptions({ inheritAttrs:false }); withDefaults(defineProps<{title?:string; subtitle?:string; bordered?:boolean}>(),{bordered:true});</script>
