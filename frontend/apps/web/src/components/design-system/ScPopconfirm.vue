<template><TDesignPopconfirm v-bind="{ ...$attrs, ...semanticPrimitiveIdentity('ScPopconfirm') }" :content="content" :confirm-btn="confirmLabel" :cancel-btn="cancelLabel" @confirm="emit('confirm')" @cancel="emit('cancel')"><slot /></TDesignPopconfirm></template>
<script setup lang="ts">import { TDesignPopconfirm } from './tdesignPrimitiveBridge'; import { semanticPrimitiveIdentity } from './primitiveAdapter'; defineOptions({ inheritAttrs:false }); withDefaults(defineProps<{content:string; confirmLabel?:string; cancelLabel?:string}>(),{confirmLabel:'确认',cancelLabel:'取消'}); const emit=defineEmits<{confirm:[];cancel:[]}>();</script>
