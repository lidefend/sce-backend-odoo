<template><TDesignAutoComplete v-bind="{ ...$attrs, ...semanticPrimitiveIdentity('ScAutoComplete') }" :value="modelValue" :options="options" :disabled="disabled" @change="emit('update:modelValue', String($event ?? ''))"><slot /></TDesignAutoComplete></template>
<script setup lang="ts">import { TDesignAutoComplete } from './tdesignPrimitiveBridge'; import { semanticPrimitiveIdentity } from './primitiveAdapter'; defineOptions({ inheritAttrs: false }); defineProps<{modelValue?:string; options?:Array<string|Record<string,unknown>>; disabled?:boolean}>(); const emit=defineEmits<{ 'update:modelValue':[value:string] }>();</script>
