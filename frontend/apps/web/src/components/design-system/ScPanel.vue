<template><component :is="props.as" class="sc-panel-host"><TDesignCard class="sc-design-panel" data-semantic-component="ScPanel" data-semantic-driver="tdesign-card" data-semantic-layer="primitive" :data-tone="props.tone" :bordered="props.tone !== 'subtle' && props.tone !== 'workspace'" :body-style="bodyStyle"><slot /></TDesignCard></component></template>
<script setup lang="ts">import { computed } from 'vue'; import { TDesignCard } from './tdesignPrimitiveBridge'; const props = withDefaults(defineProps<{as?:string;tone?:'default'|'subtle'|'raised'|'workspace'}>(),{as:'section',tone:'default'}); const bodyStyle = computed(() => ({ padding: props.tone === 'workspace' ? '0' : 'calc(var(--sc-component-panel-padding) * 1px)' }));</script>
<style scoped>.sc-design-panel[data-tone='subtle']{background:var(--sc-app-subtle-bg);box-shadow:none}.sc-design-panel[data-tone='raised']{box-shadow:var(--sc-app-shadow-popover)}.sc-design-panel[data-tone='workspace']{background:transparent;box-shadow:none}</style>
