# U-B：客户业务分组收尾与三个代表面迁移

状态：active；前一候选 8907a41ff08b7ceca25acef8378ca0df40a3d84f；统一基线 9f7bb560917a98b638a6d2a516fe2542634fdff8。

## 边界与顺序

1. P1 / smart_construction_core：客户原生视图分为基本资料、工商信息、联系方式、账户与财务；保留集合全宽。核实字段关系，不改存储或金额规则。
2. P1 / 原生视图及业务配置：付款、投标、项目看板迁移；保留语义增强、可选明细与金额绑定、中标快照、看板只读和项目编辑能力。
3. P0 / smart_core 与通用前端：只修有事实支撑的通用结构或版本兼容缺陷，不猜业务字段归属。
4. P4 / 现有台账与浏览器入口：沿用 89 个菜单台账，仅解六个未决及本批受影响入口；不新增运行环境和业务功能。

标准行业信息组织属于 P1，不放前端、P0、客户模块或运行配置；现有合同与权限不放宽。P4 只采证。

## 验证计划

风险：中（行业视图与配置迁移，涉及共有模型的 action 隔离）。最早 L0 完整指纹，继而 L1 ci.local.iteration 与原生/结构守卫，L2 非零受影响契约和能力测试，L3 受管增量升级，L4 1088/390 受影响页面，L5 生成证据预检、冻结后一次 Quick 和独立复核。

旧客户创建态证据保留；变化的章节不能复用旧截图。新检查聚焦分组、记录 56 查看/编辑和三个代表面，跳过无关保存旅程（不改存储或保存规则）。未受影响的测试须以完整指纹和确定性影响分析承接。

运行身份：既有 local.dev / sc-local-dev / sc_dev_demo / ^sc_dev_demo$ / sc_local_dev_odoo_data；凭据仅来自受管 .env.dev。只进行所需模块升级及已登记验证；不重建数据库或自动重置用户记录。

兼容计数维持 system_admin / company 1 / model-create 同口径，基数 49；其他状态单列覆盖。消费者和未决归零并覆盖正式变体后才退役兼容重组与豁免。

## 首轮实现与定向恢复

- P1 客户七章：基本资料、工商信息、联系方式、账户与财务、联系人、账户明细、附件与备注。
- company_type 为原生 compute/inverse 界面字段，is_company 为主体事实；运行 registry 与切换测试通过。两种税率字段各自独立存储；没有自动同步。账户摘要字段供付款/投标带入，bank_ids 是独立多账户集合，合同优先摘要再回退集合；不变更存储或金额。
- P1 投标原生 view_tender_bid_form 的原 notebook 转为全页业务组；中标组的动态可见、只读、确认快照和动作保持。
- 两份 generated 纯顺序配置经精确 XMLID 的 P1 数据声明停用（安装/升级均执行）；原数据保留以供回滚。其余五份配置抽出 readonly/visible 等非结构策略，移除章节、分组和 sequence；投标过程集合的旧隐藏退役，原生状态/权限继续约束。
- P0 结构诊断区分字段顺序/归属和稀疏语义策略，后者不独占结构；新增测试验证可叠加帮助/只读且不变动原生节点顺序，sequence 仍触发冲突。
- L1 静态通过；L2 156 测试通过；L3 受管 smart_core/smart_construction_core 升级通过。定向模块首轮19项中3项为旧结构断言失败，16项通过；更新断言至原生树/控件状态后仅恢复3项并全部通过。包含客户记录态分组与字段权威、付款明细金额绑定、中标事实与动作、看板只读隔离。失败及恢复保留于 artifacts/form-structure-unification/ub-module-tests*.log。
- 浏览器和 Quick 尚未运行，不登记四面通过。
