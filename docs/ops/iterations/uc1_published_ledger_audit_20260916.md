# UC1 发布台账核对

状态：已核对，兼容消费者发布数由 50 更新为 46。

## 核对边界

- 发布基线：`main@28b7695dc9f1cebbbe9bd5d715e951c6dec8a4da`。
- 核对来源：已合入的
  `docs/ops/iterations/uc1_contract_settlement_native_migration_20260915.md`、同一发布提交中的
  P1 XML／分类模板和定向契约测试。
- 本次只复核合入内容与已有交付记录，不执行 UC1 Quick，不重跑浏览器验收，也不重造截图。

## 四入口结果

| 入口 | action / view | 合入源码核对 | 已记录运行结论 | 台账处理 |
|---|---|---|---|---|
| 收入合同 | 609 / 1569 | 原生容器树；配置章节为空；兼容依赖为空 | 迭代记录登记页面通过 | 移除消费者 |
| 支出合同 | 610 / 1570 | 原生容器树；配置章节为空；兼容依赖为空 | 迭代记录登记页面通过 | 移除消费者 |
| 收入结算 | 781 / 1764 | 原生容器树；配置章节为空；兼容依赖为空 | 迭代记录登记页面通过并获接受 | 移除消费者 |
| 支出结算 | 782 / 1764 | 原生容器树；配置章节为空；兼容依赖为空 | 迭代记录登记页面通过并获接受 | 移除消费者 |

这次扣减依据是四入口在发布源码中的实际结构退役，以及已合入迭代记录中的页面验收结论；PR 合并状态本身不作为扣减依据。四项均满足后，发布台账由 50 扣减为 46。

## 证据限制

UC1 工作树清理时，其忽略目录中的浏览器身份记录、运行日志和截图一并删除，现已不可复查。本文不把 PR 或 CI 状态描述为原始浏览器证据，也不以新截图替代原候选证据。可继续复核的材料仅包括发布提交、GitHub 门禁记录和已合入迭代记录。

action 777 保持原“环境阻断、未通过”状态，本次没有探测或改写。
