# 编码规则能力基线 v1

编码规则直接使用 Odoo `ir.sequence` 作为唯一编号权威。行业配置管理员只能查看产品业务
序列，并仅能修改前缀、后缀、位数、步长和下一流水号；规则名称、技术键、公司归属、创建
和删除均由产品发布治理。

产品业务范围由后端 `sc_product_configurable` 判定和记录规则强制过滤。前端只渲染动作契约，
不能传入任意序列键或绕过能力模型。
