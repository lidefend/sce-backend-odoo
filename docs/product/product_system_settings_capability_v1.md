# 产品系统参数能力基线 v1

“系统参数”不再复用 UI 契约列表，也不直接开放 Odoo 全量 `ir.config_parameter`。正式入口
使用 `sc.product.system.settings` 临时门面，只暴露产品批准的参数；底层参数仍是唯一运行
权威，不复制配置事实。

首个正式参数是成本台账自动归集来源。用户只能在会计凭证、采购订单、库存入库和暂不
自动归集中四选一，从结构上避免多个来源同时启用造成重复成本。认证密钥、数据库标识、
部署地址等运行秘密严格保留在部署层，不进入产品页面或前端契约。
