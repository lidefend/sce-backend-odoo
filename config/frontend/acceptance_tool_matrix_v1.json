{
  "schema": "frontend_acceptance_tool_matrix.v1",
  "tools": {
    "delivery-hardening": {"profiles": ["local", "test"], "operations": ["readonly", "simulated-write"]},
    "full-product-audit": {"profiles": ["local", "test", "daily"], "operations": ["readonly"]},
    "form-system-audit": {"profiles": ["local", "test"], "operations": ["isolated-write"], "fixture_required": true},
    "geometry-scroll-audit": {"profiles": ["local", "test", "daily"], "operations": ["readonly"]},
    "dynamic-list-optional-columns": {"profiles": ["local", "test", "daily"], "operations": ["readonly"]},
    "daily-release-probe": {"profiles": ["daily"], "operations": ["readonly"], "target_identity_required": true},
    "production-safe-smoke": {"profiles": ["production"], "operations": ["production-safe-smoke"], "target_identity_required": true, "redaction_required": true},
    "acceptance-report-server": {"profiles": ["local", "test"], "operations": ["managed-service"]}
  }
}
