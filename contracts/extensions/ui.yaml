# 保留扩展层，当前首阶段仅说明：UI 仅消费 formStructureRole/component元信息。
ui:
  render_profile:
    - readonly
    - create
    - edit
  semantic_components:
    - sc.display.text
    - sc.table.data
    - sc.select.remote
